export default function AdminOverviewPage() {
  return (
    <section className="card">
      <h2>Admin workspace</h2>
      <p className="muted">
        Platform-admin access is granted only through database bootstrap and future admin-approved changes.
      </p>
    </section>
  );
}
